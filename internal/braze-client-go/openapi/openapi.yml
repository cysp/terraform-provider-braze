openapi: 3.0.3
info:
  title: Braze REST API
  version: "0.1"
components:
  securitySchemes:
    brazeApiKey:
      type: http
      scheme: bearer
      bearerFormat: Braze

security:
  - brazeApiKey: []

paths:
  /content_blocks/list:
    get:
      summary: List available Content Blocks
      description: List your existing Content Blocks information
      operationId: listContentBlocks
      tags:
        - Content Blocks
      parameters:
        - name: modified_after
          in: query
          description: Retrieve only Content Blocks updated at or after the given time
          required: false
          schema:
            type: string
            format: date-time
        - name: modified_before
          in: query
          description: Retrieve only Content Blocks updated at or before the given time
          required: false
          schema:
            type: string
            format: date-time
        - name: limit
          in: query
          description: Maximum number of Content Blocks to retrieve (default 100, max 1000)
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - name: offset
          in: query
          description: Number of Content Blocks to skip before returning results
          required: false
          schema:
            type: integer
            minimum: 0
            default: 0
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: './schemas/content_blocks/list/response.yml#/ListContentBlocksResponse'
        default:
          $ref: './responses/error.yml#/ErrorResponse'

  /content_blocks/info:
    get:
      summary: See Content Block information
      description: Call information for your existing Content Blocks
      operationId: getContentBlockInfo
      tags:
        - Content Blocks
      parameters:
        - name: content_block_id
          in: query
          description: Content Block identifier
          required: true
          schema:
            type: string
        - name: include_inclusion_data
          in: query
          description: When set to true, returns Message Variation API identifiers where this Content Block is included
          required: false
          schema:
            type: boolean
            default: false
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: './schemas/content_blocks/info/response.yml#/GetContentBlockInfoResponse'
        default:
          $ref: './responses/error.yml#/ErrorResponse'

  /content_blocks/create:
    post:
      summary: Create Content Block
      description: Create a Content Block on the Braze dashboard
      operationId: createContentBlock
      tags:
        - Content Blocks
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: './schemas/content_blocks/create/request.yml#/CreateContentBlockRequest'
      responses:
        '201':
          description: Content Block created successfully
          content:
            application/json:
              schema:
                $ref: './schemas/content_blocks/create/response.yml#/CreateContentBlockResponse'
        default:
          $ref: './responses/error.yml#/ErrorResponse'

  /content_blocks/update:
    post:
      summary: Update Content Block
      description: Update a Content Block on the Braze dashboard
      operationId: updateContentBlock
      tags:
        - Content Blocks
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: './schemas/content_blocks/update/request.yml#/UpdateContentBlockRequest'
      responses:
        '200':
          description: Content Block updated successfully
          content:
            application/json:
              schema:
                $ref: './schemas/content_blocks/update/response.yml#/UpdateContentBlockResponse'
        default:
          $ref: './responses/error.yml#/ErrorResponse'

  /templates/email/list:
    get:
      summary: List available email templates
      description: Get a list of available email templates in your Braze account
      operationId: listEmailTemplates
      tags:
        - Email Templates
      parameters:
        - name: modified_after
          in: query
          description: Retrieve only templates updated at or after the given time
          required: false
          schema:
            type: string
            format: date-time
        - name: modified_before
          in: query
          description: Retrieve only templates updated at or before the given time
          required: false
          schema:
            type: string
            format: date-time
        - name: limit
          in: query
          description: Maximum number of templates to retrieve (default 100, max 1000)
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 100
        - name: offset
          in: query
          description: Number of templates to skip before returning results
          required: false
          schema:
            type: integer
            minimum: 0
            default: 0
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: './schemas/templates/email/list/response.yml#/ListEmailTemplatesResponse'
        default:
          $ref: './responses/error.yml#/ErrorResponse'

  /templates/email/info:
    get:
      summary: See email template information
      description: Get information on your email templates
      operationId: getEmailTemplateInfo
      tags:
        - Email Templates
      parameters:
        - name: email_template_id
          in: query
          description: Email template API identifier
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: './schemas/templates/email/info/response.yml#/GetEmailTemplateInfoResponse'
        default:
          $ref: './responses/error.yml#/ErrorResponse'

  /templates/email/create:
    post:
      summary: Create email template
      description: Create email templates on the Braze dashboard
      operationId: createEmailTemplate
      tags:
        - Email Templates
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: './schemas/templates/email/create/request.yml#/CreateEmailTemplateRequest'
      responses:
        '201':
          description: Template created successfully
          content:
            application/json:
              schema:
                $ref: './schemas/templates/email/create/response.yml#/CreateEmailTemplateResponse'
        default:
          $ref: './responses/error.yml#/ErrorResponse'

  /templates/email/update:
    post:
      summary: Update email template
      description: Update email templates on the Braze dashboard
      operationId: updateEmailTemplate
      tags:
        - Email Templates
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: './schemas/templates/email/update/request.yml#/UpdateEmailTemplateRequest'
      responses:
        '200':
          description: Template updated successfully
          content:
            application/json:
              schema:
                $ref: './schemas/templates/email/update/response.yml#/UpdateEmailTemplateResponse'
        default:
          $ref: './responses/error.yml#/ErrorResponse'
